require 'sinatra/activerecord/rake'
require './app'

# use the command rake in terminal to make a 
# migrate folder with the create table def inside: 
# rake db:create_migration NAME=create_users_table
